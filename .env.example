# Cliente (Vite — expostas no bundle; use apenas chave publishable)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Servidor (server functions / SSR — mesmas credenciais publishable)
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
